## Task:
Write a function that adds two integers and returns the result.

- Prototype: int add(int, int);



## Implementation:

I'm asked to create a function that adds two interges and retuns the result.

To implements the function `int add`, my fuction will have to meet the following requirements:

1. **Function** `int add(int, int)`: The function take two integers `n` as an 
parameters (input).


```c
int add(int num1, int num2)
```

2. **Return**: The sum of the two integers.


```c
return ((num1 + num2));
```



## Pseudocode:

```ps
FUNCTION add(num1, num2):
        RETURN num1 + num2
```