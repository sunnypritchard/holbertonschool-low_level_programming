#include <stdio.h>
/**
 * main - Entry point
 * @void: no paramater
 *
 * Description: Uses prinf to output the str:
 *
 * with proper grammar, but the outcome is a piece of art
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art");
	return (0);
}
