#include <stdio.h>

/**
 * main - Entry point
 * @void: No parameters
 *
 * Description: Prints a string using the printf function.
 *              Outputs the message "with proper grammar, but the outcome is a piece of art,"
 *              followed by a newline to the standard output.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
